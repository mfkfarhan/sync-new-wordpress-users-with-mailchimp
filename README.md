# sync-new-wordpress-users-with-mailchimp

This plugin sends user's first name, last name, email address and birth_date (meta field) to mailchimp every time a new user is created or a user is updated.
